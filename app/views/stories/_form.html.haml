= simple_form_for @story, :html => { :class => 'form-horizontal' } do |f|
  = f.input :headline
  = f.input :link
  = f.input :slug
  = f.input :date
  = f.association :outlets

  .form-actions
    = f.button :submit
